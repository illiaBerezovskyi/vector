# Vektor Agency

Marketing agency landing page — CRM, advertising, AI and analytics services.

Built with React + Vite. Fully static, no backend required.

## Development

```bash
npm install
npm run dev      # local dev server at http://localhost:5173
npm run build    # production build in dist/
```

## Deployment

```bash
npm run deploy   # build and publish to GitHub Pages
```
